info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/examples/1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Delete Example
  Deletes an existing Example resource from the database by its ID.

  ## Response Examples

  ### Success (200 OK)
  ```json
  {
    "ok": true
  }
  ```

  ### Error (404 Not Found)
  ```json
  {
    "success": false,
    "err_code": "NOT_FOUND",
    "message": "Example not found"
  }
  ```
